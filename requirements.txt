# Core (all modes)
requests>=2.31.0
boto3>=1.34.0

# SSF mode (JWT signing)
pyjwt>=2.8.0
cryptography>=41.0.0

# Real mode (browser automation)
playwright>=1.40.0
pyotp>=2.9.0

# Formatting
colorama>=0.4.6
tabulate>=0.9.0
